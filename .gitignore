# Python
venv/
__pycache__/
*.pyc
*.pyo
.pytest_cache/

# macOS
.DS_Store

# Runtime data / logs (regenerated; large, machine-local)
logs/
*.db
*.db-shm
*.db-wal
*.zip
# Raw WEC live captures (--record output; machine-local race data)
*.jsonl.gz

# FastF1 API cache (large, machine-local)
data/fastf1_cache/

# Keep static config that lives under data/
!data/entries_*.json

# Sample race archives bundled for offline replay demo
!sample-archives/*.zip

# Local demo output (screenshots/video), not part of the repo
demo-screenshots/
